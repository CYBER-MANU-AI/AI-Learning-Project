walker reset_graph {
    can reset with entry {
           print("Reset helper: to fully reset the graph, restart Jaseci or clear the DB.");
           print("If you want a destructive reset, run the CLI DB clear or remove nodes manually.");
    }
}
